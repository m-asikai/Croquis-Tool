<Page x:Class="Wpf_test.ImageViewer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Wpf_test"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Page1">


        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="90*" />
                <RowDefinition Height="10*" />
            </Grid.RowDefinitions>
        <Image Grid.Row="0" Name="ImageBox" Stretch="Uniform" HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch"  RenderOptions.BitmapScalingMode="HighQuality" Margin="5 5 5 0"/>


        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Height="40" Margin="0 2 0 0">
            
            
            <StackPanel Name="MinimalButtons" Orientation="Horizontal" Visibility="Collapsed">
                <StackPanel Margin="0 0 10 0">
                    <Button Content="Show" Click="HideButton_Click" FontSize="10" Margin="0 0 0 5"/>
                    <CheckBox Content="On top" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" FontSize="10" Margin="0 0 0 5"/>
                </StackPanel>
                <TextBlock Name="MinTimer" Text="{Binding TimerText}" VerticalAlignment="Center" FontWeight="Bold" FontSize="16" Margin="5 0 0 0"/>
            </StackPanel>
            
            
            <StackPanel Name="ControlPanel" Orientation="Horizontal" Loaded="ControlPanel_Loaded">

                <StackPanel>
                    <CheckBox Content="On top" Checked="CheckBox_Checked" Unchecked="CheckBox_Unchecked" FontSize="10" Margin="0 0 0 5"/>
                    <Button Name="HideButton" Content="Hide" Click="HideButton_Click" FontSize="8"/>
                </StackPanel>
                <Button Style="{DynamicResource IconButton}" Name="Back"  Width="50" Click="Back_Click">
                    <Image Source="/Icons/backward.png" Width="24" Height="24" />
                </Button>
                <Button Style="{DynamicResource IconButton}" Name="Pause"  Width="50" Click="Pause_Click">
                    <Image Source="/Icons/pause.png" Width="24" Height="24" />
                </Button>
                <TextBlock Text="{Binding TimerText}" FontSize="24" VerticalAlignment="Center" TextAlignment="Center"/>
                <Button Style="{DynamicResource IconButton}" Name="Stop"  Width="50" Click="Stop_Click">
                    <Image Source="/Icons/stop.png" Width="24" Height="24" />
                </Button>
                <Button Style="{DynamicResource IconButton}" Name="Forward" Width="50" Click="Forward_Click">
                    <Image Source="/Icons/forward.png" Width="24" Height="24" />
                </Button>
            </StackPanel>
        </StackPanel>
            
        </Grid>
</Page>
